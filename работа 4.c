#include <stdio.h>
#include <stdlib.h>
#define n 20
int s()
{
    int s=0;
    for(i=1,i<=n,i++)
        printf("¬ведите %d -последовательность\n", &n)
        scanf("%f &f &f", &a[i],&b[i],&c[i]);
    return s
}
int main()
{
   setlocale(0,"");
   int a[n], b[n], c[n], i;
   float x[n]; y[n];

return 0;
}

